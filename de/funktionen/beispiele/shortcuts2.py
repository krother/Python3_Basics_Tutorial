
# Shortcuts - Exercise 2
#
# Simplify the code using one of the functions `enumerate(), sum(), range(), zip()`:

# Remark: all those functions return an iterator. You need to apply list() to see the result,
# e.g. print(list(range(10)))


i = 0
while i < 10:
    print(i * '*')
    i += 1
